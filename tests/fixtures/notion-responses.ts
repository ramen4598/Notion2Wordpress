// Description: Mock Notion API responses for testing

export const mockNotionPage = {
  // : Add mock Notion page data
};

export const mockNotionBlocks = {
  // : Add mock Notion blocks data
};

export const mockNotionDatabase = {
  // : Add mock Notion database data
};

// TODO: status라는 이름의 status는 피하는 것이 좋을 듯
// TODO: enum들 간의 표현 방식 통일 고려
export enum NotionPageStatus {
    Writing = 'writing',
    Adding = 'adding',
    Error = 'error',
    Complete = 'complete',
}